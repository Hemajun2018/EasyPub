/**
 * Keep client/server defaults aligned.
 *
 * Default is disabled to avoid impacting real users.
 * Set AUTH_GET_SESSION_MIN_INTERVAL_MS and
 * NEXT_PUBLIC_AUTH_GET_SESSION_MIN_INTERVAL_MS to enable explicitly.
 */
export const DEFAULT_AUTH_GET_SESSION_MIN_INTERVAL_MS = 0;
